$(".wrapper__contact__input__ok").click(function(){ 
        $(".wrapper__contact").fadeOut(); 
        
      }); 
    });

// Вот только не знаю как его увязать с валидацией, 
//без этого он безполезен (потому и не подключен)